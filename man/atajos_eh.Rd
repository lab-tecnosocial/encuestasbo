% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/aliases.R
\name{atajos_eh}
\alias{atajos_eh}
\alias{get_personas_eh}
\alias{get_viviendas_eh}
\title{Atajos de acceso a la Encuesta de Hogares por nivel}
\usage{
get_personas_eh(
  anio,
  departamento = NULL,
  area = NULL,
  variables = NULL,
  as = c("arrow", "tibble", "duckdb"),
  overwrite = FALSE,
  verbose = TRUE
)

get_viviendas_eh(
  anio,
  departamento = NULL,
  area = NULL,
  variables = NULL,
  as = c("arrow", "tibble", "duckdb"),
  overwrite = FALSE,
  verbose = TRUE
)
}
\arguments{
\item{anio}{Entero. Año de la encuesta (`2012`-`2024`).}

\item{departamento}{Vector. Código(s) `1`-`9` o nombre(s) del departamento
(e.g., `"Santa Cruz"`). Si `NULL`, incluye todos.}

\item{area}{Vector. `1`/`"Urbana"` o `2`/`"Rural"`. Si `NULL`, incluye ambas.}

\item{variables}{Vector de caracteres. Nombres de columnas a seleccionar.
Si `NULL`, devuelve todas. Las columnas de identificación, geografía y
diseño muestral (`folio`, `depto`, `area`, `factor`, `upm`, `estrato`)
siempre se incluyen.}

\item{as}{Formato de retorno: `"arrow"` (lazy, por defecto), `"tibble"`
(RAM) o `"duckdb"` (conexión DBI con la tabla registrada).}

\item{overwrite}{Lógico. Si `TRUE`, re-descarga aunque exista en caché.}

\item{verbose}{Lógico. Mostrar mensajes de progreso. Por defecto `TRUE`.}
}
\value{
Lo mismo que [get_eh()] según `as`.
}
\description{
Wrappers de [get_eh()] para los niveles de análisis más comunes.
}
\examples{
\dontrun{
get_personas_eh(2023, departamento = "La Paz")
get_viviendas_eh(2023)
}
}
