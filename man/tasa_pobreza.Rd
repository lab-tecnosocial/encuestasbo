% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/indicadores.R
\name{tasa_pobreza}
\alias{tasa_pobreza}
\title{Tasa de pobreza (EH)}
\usage{
tasa_pobreza(
  anio,
  extrema = FALSE,
  por = NULL,
  vartype = "ci",
  verbose = FALSE
)
}
\arguments{
\item{anio}{Entero. Año de la EH.}

\item{extrema}{Lógico. Si `TRUE`, pobreza extrema (`pobre_extremo`); si
`FALSE` (defecto), pobreza moderada (`pobre`).}

\item{por}{Caracteres. Variable(s) de desagregación (p. ej. `"depto"`,
`"area"`, `c("depto","area")`). `NULL` (defecto) = total nacional.}

\item{vartype}{Tipo de varianza para `srvyr` (`"ci"` por defecto).}

\item{verbose}{Lógico. Mostrar progreso de descarga.}
}
\value{
Un tibble con la(s) variable(s) de `por`, `tasa` y su varianza.
}
\description{
Incidencia de pobreza monetaria ponderada con el diseño muestral de la EH.
}
\examples{
\dontrun{
tasa_pobreza(2023)                       # nacional
tasa_pobreza(2023, por = "area")         # por área
tasa_pobreza(2023, extrema = TRUE, por = "depto")
}
}
\seealso{
[diseno_eh()], [tasa_desempleo()].
}
